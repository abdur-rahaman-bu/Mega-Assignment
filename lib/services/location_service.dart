/// LocationService is kept for future use (e.g. when a Google Maps API key
/// is added). Currently unused — address entry is done via manual text input.
class LocationService {
  LocationService();
}
